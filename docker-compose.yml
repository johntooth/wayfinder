services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: wayfinder-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: wayfinder
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  storage:
    image: minio/minio:latest
    container_name: wayfinder-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 6

  # Optional semantic-chunking sidecar (ADR-030). Start with:
  #   docker compose --profile semchunk up
  # then set CHUNKER_PROVIDER=semchunk and SEMCHUNK_URL=http://localhost:8000
  # on the web app. The default profile is unaffected when this is off.
  semchunk:
    build: ./services/semchunk
    container_name: wayfinder-semchunk
    profiles: ["semchunk"]
    restart: unless-stopped
    ports:
      - "8000:8000"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz', timeout=4)",
        ]
      interval: 10s
      timeout: 5s
      retries: 6

  langfuse:
    image: langfuse/langfuse:2
    container_name: wayfinder-langfuse
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "3030:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/langfuse
      NEXTAUTH_URL: http://localhost:3030
      NEXTAUTH_SECRET: replace-me
      SALT: replace-me
      TELEMETRY_ENABLED: "false"

volumes:
  postgres-data:
  minio-data:
